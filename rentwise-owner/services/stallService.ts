export const getStalls = async () => {
  // TODO: fetch stalls from API
  return Promise.resolve([] as any[]);
};

export const getStallById = async (id: string) => {
  // TODO: fetch stall details
  return Promise.resolve(null);
};
